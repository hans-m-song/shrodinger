# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Playbook {
  playbookId: Int!
  filepath: String!
  contents: JSON!
  playbookRuns(limit: Int = 10, offset: Int = 0, playbookId: Int, playbookRunId: Int): [PlaybookRun!]!
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

type PlaybookRun {
  playbookRunId: Int!
  playbookId: Int!
  status: PlaybookRunStatus!
  contents: String!
  playbook: Playbook!
}

enum PlaybookRunStatus {
  Pending
  Running
  Completed
  Failed
}

type Query {
  playbooks(limit: Int = 10, offset: Int = 0, playbookId: Int): [Playbook!]!
  playbookRuns(limit: Int = 10, offset: Int = 0, playbookId: Int, playbookRunId: Int): [PlaybookRun!]!
}

type Mutation {
  createPlaybook(filepath: String!, contents: JSON!): Playbook!
  createPlaybookRun(playbookId: Int!): PlaybookRun!
}