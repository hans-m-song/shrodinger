# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type PlaybookRun {
  playbookRunId: Int!
  playbookId: Int!
  status: PlaybookRunStatus!
  contents: String!
  createdAt: Int!
  modifiedAt: Int!
  startedAt: Int!
  completedAt: Int!
  playbook: Playbook!
}

enum PlaybookRunStatus {
  Pending
  Running
  Completed
  Failed
}

type Playbook {
  playbookId: Int!
  filepath: String!
  contents: JSON!
  createdAt: Int!
  modifiedAt: Int!
  playbookRuns(limit: Int = 10, offset: Int = 0, playbookId: Int, playbookRunId: Int): [PlaybookRun!]!
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

type Query {
  playbooks(limit: Int = 10, offset: Int = 0, filepath: Int): [Playbook!]!
  playbookRuns(limit: Int = 10, offset: Int = 0, playbookId: Int, playbookRunId: Int): [PlaybookRun!]!
}

type Mutation {
  createPlaybook(filepath: String!, contents: JSON!): Playbook!
  createPlaybookRun(playbookId: Int!, status: PlaybookRunStatus! = Pending, contents: String! = ""): PlaybookRun!
}